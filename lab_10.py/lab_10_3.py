string = ["Nimble", "fox", "jumped", "through", "lazy", "dog", "."]

#joining all elements in one line 
string = " ".join(string)

#remove the space before the dot 
string = string[0: -2] + "."

print(string)

# number 4 
# These methods simplify the handling of incoming data. 
# Unfortunately, there are many methods with different data, so it is impossible to remember them all.
